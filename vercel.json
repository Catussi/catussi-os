{
  "buildCommand": "npm run build",
  "outputDirectory": "out",
  "framework": null,
  "installCommand": "YARN_PRODUCTION=false NODE_OPTIONS=--openssl-legacy-provider yarn install --ignore-scripts"
}
